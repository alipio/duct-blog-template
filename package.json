{
  "name": "duct-blog",
  "version": "1.0.0",
  "description": "Duct Bootstrap Template",
  "dependencies": {
    "bootstrap": "^3.4.1"
  },
  "devDependencies": {
    "browser-sync": "^2.2.0",
    "gulp-fs": "0.0.2",
    "gulp-sass": "^2.2.0",
    "gulp-template": "^4.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
